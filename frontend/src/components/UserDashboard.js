import React from 'react';

const UserDashboard = () => {
    return (
        <div className="dashboard">
            <h1>Driver Dashboard</h1>
            {/* Add trip management components */}
        </div>
    );
};

export default UserDashboard;